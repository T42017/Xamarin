<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Enigma"
             x:Class="Enigma.MainPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <local:ActuatorControl Grid.Row="0" ActuatorRotation="{Binding ActuatorRotation}"></local:ActuatorControl>
            <Label Grid.Row="1" Text="{Binding ActuatorRotation}"></Label>
            <Button Grid.Row="2" Clicked="Button_OnClicked"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>